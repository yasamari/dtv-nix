diff --git a/server/app/constants.py b/server/app/constants.py
--- a/server/app/constants.py
+++ b/server/app/constants.py
@@ -1,6 +1,7 @@
 
 import base64
 import hashlib
+import os
 import pkgutil
 import secrets
 import sys
@@ -28,11 +29,14 @@
 CLIENT_DIR = BASE_DIR.parent / 'client/dist'
 
 # データディレクトリ
-DATA_DIR = BASE_DIR / 'data'
+DATA_DIR = Path(os.getenv('KONOMITV_DATA_DIR', str(BASE_DIR / 'data')))
+DATA_DIR.mkdir(parents=True, exist_ok=True)
 ## アカウントのアイコン画像があるディレクトリ
 ACCOUNT_ICON_DIR = DATA_DIR / 'account-icons'
+ACCOUNT_ICON_DIR.mkdir(parents=True, exist_ok=True)
 ## サムネイル画像があるディレクトリ
 THUMBNAILS_DIR = DATA_DIR / 'thumbnails'
+THUMBNAILS_DIR.mkdir(parents=True, exist_ok=True)
 ## Twitter 関連のデバッグ用スクリーンショットの保存先ディレクトリ
 TWITTER_DEBUG_SCREENSHOTS_DIR = DATA_DIR / 'twitter-debug-screenshots'
 ## デバッグ用スクリーンショットの保持期限 (日数)
@@ -51,7 +55,8 @@
 JIKKYO_CHANNELS_PATH = STATIC_DIR / 'jikkyo_channels.json'
 
 # ログディレクトリ
-LOGS_DIR = BASE_DIR / 'logs'
+LOGS_DIR = Path(os.getenv('KONOMITV_LOGS_DIR', str(BASE_DIR / 'logs')))
+LOGS_DIR.mkdir(parents=True, exist_ok=True)
 ## サーバーログのアーカイブ（日付別ログ）を格納するサブディレクトリ
 ## ログディレクトリ直下にアーカイブが大量に並ぶとノイズになるため、サブディレクトリに分離する
 LOGS_ARCHIVES_DIR = LOGS_DIR / 'archives'
diff --git a/server/app/config.py b/server/app/config.py
--- a/server/app/config.py
+++ b/server/app/config.py
@@ -1,5 +1,6 @@
 
 import asyncio
+import os
 import concurrent.futures
 import platform
 import re
@@ -360,7 +361,7 @@
 # _CONFIG には直接アクセスせず、Config() 関数を通してアクセスする
 
 _CONFIG: ServerSettings | None = None
-_CONFIG_YAML_PATH = BASE_DIR.parent / 'config.yaml'
+_CONFIG_YAML_PATH = Path(os.getenv('KONOMITV_CONFIG_YAML_PATH', str(BASE_DIR.parent / 'config.yaml')))
 _DOCKER_PATH_PREFIX = '/host-rootfs'
